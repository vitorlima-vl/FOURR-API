test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

google:
  service: GCS
  project: fourr
  credentials: <%= Rails.application.credentials.gcs_fourr_app.to_json %>
  bucket: fourr-app

google_dev:
  service: GCS
  project: fourr
  credentials: <%= Rails.root.join("config/secrets/fourr-app.json") %>
  bucket: fourr-app
